print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
print("USE NEW VERSION, CHECK DISCORD THREAD")
